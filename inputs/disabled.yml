configuration:
  disabled_input: Untouchable
valid:
  null_disabled:
  empty_disabled: ''
  disabled: any string
options:
  null_disabled:
  string_disabled:

_inputs:
  disabled_input:
    type: disabled
  options.null_disabled:
    options:
      empty_type: 'null'
  options.string_disabled:
    options:
      empty_type: string
