configuration:
  disabled_input: Untouchable
options:
  null_disabled:
  string_disabled:

_inputs:
  disabled_input:
    type: disabled
  options.null_disabled:
    type: disabled
    options:
      empty_type: 'null'
  options.string_disabled:
    type: disabled
    options:
      empty_type: string
